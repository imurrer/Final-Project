//main.cpp

#include "snowman.h"

int main(){
  bool loop = true;
  char c;
  
  gfx_open(500, 700, "Frosty on the Run");
  
  while (loop) {
    c = gfx_wait();
    gfx_clear();
    switch(c) {
      case 'p' : // p to play game
        break;
      case '
  
  
  
  
}
